import React from 'react';

const SideBar = () => {
    return (
        <div>
            <button>Home</button>
            <button>phone</button>
            <button>contact</button>
        </div>
    );
}

export default SideBar;
